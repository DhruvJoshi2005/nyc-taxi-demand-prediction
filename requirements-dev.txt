# Full development stack: the app's runtime deps PLUS the data pipeline & notebooks.
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dev.txt
-r requirements.txt

dask[dataframe]==2026.7.0
pyyaml==6.0.3
seaborn==0.13.2
mlflow==3.14.0
dvc==3.67.1
jupyter==1.1.1
ipykernel==7.3.0

# Optional, add later only if needed: xgboost, optuna, pytest
